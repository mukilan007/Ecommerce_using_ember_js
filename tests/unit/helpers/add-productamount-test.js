import { addProductamount } from '../../../helpers/add-productamount';
import { module, test } from 'qunit';

module('Unit | Helper | add productamount');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = addProductamount(42);
  assert.ok(result);
});
